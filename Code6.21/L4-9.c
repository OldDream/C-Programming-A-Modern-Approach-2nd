#include <stdio.h>
int main(void)
{
	int i=0;
	int sum=0;
	int num,tmp;

	printf("How many num do you want?");
	scanf("%d",&num);

	while(i<num)
		putchar('*');
	while(no-->=0)
	putchar('\n');

	return 0;
}